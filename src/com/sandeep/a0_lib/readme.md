# Library for My DSA Learning

---

## Functions added in my library
- String readString(String str);
- int readInt(Scanner in);  //these read string--> trim--> splits--> parseInt --> if error happens (read again : while (true))
- int[] readArray(Scanner in); 